# Gallery assets

Low-resolution JPEG stills (RAW left / PE right) for the GitHub homepage. Regenerate with:

```bash
scripts/make_gallery_thumbs.sh
```

Do not commit full `.mp4` outputs; they are gitignored.
