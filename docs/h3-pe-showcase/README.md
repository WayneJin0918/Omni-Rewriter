# H3 PE showcase

Public landing page for Omni-Rewriter's MiniMax-H3 prompt-expansion profile.

- Open [`index.html`](index.html) locally or via GitHub Pages / docs browsing.
- Low-res RAW vs PE GIF thumbs live in `thumbs/` (no full `.mp4` blobs).
- Regenerate thumbs from the gitignored experiment videos:

```bash
scripts/make_h3_pe_showcase_thumbs.sh
```

Published pairs: **s01–s10** from `experiments/t2va-base-15s-raw-vs-pe/`.
Camera-stress scenarios **s11–s16** are prepared in that experiment (prompts ready);
videos are deferred until an H3 serve path is available.
