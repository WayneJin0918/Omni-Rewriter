# Serve helpers

Canonical local Writer / optional generation runtime scripts.

Compatibility wrappers remain at `scripts/serve_*.sh` and call into this folder.
See [../README.md](../README.md) for environment prefixes.
